# NanoVMS Status - 2026-06-10

## Branch State

- Repository: `/Users/kooshapari/CodeProjects/Phenotype/repos/nanovms`
- Branch: `chore/nanovms-hygiene-updates`
- Upstream: `origin/chore/nanovms-hygiene-updates`
- HEAD: `6443a48` - `ci: add Python SDK and desktop build jobs to CI workflow`
- HEAD timestamp: `2026-06-08 21:43:15 -0700`
- Working tree: dirty before this report; existing modified file is `package-lock.json`.
- This audit writes only `STATUS_2026_06_10.md`.

## Build Matrix (go build ./...)

| Target | Command | Local result | Notes |
| --- | --- | --- | --- |
| Local macOS arm64 toolchain | `env GOCACHE=/private/tmp/nanovms-gocache go build ./...` | Pass, exit 0, 104s | Local Go reports `go1.26.2 darwin/arm64`; `go.mod` declares `go 1.23`. Command emitted a module-cache stat warning for `/Users/kooshapari/go/pkg/mod/cache/download/github.com/kooshapari/nanovms/@v/...` but still exited 0. |
| CI Linux primary | `.github/workflows/*.yml` with `actions/setup-go` `go-version: "1.23"` | Configured, not executed locally | Workflows use Ubuntu runners; local audit did not run inside Linux/KVM/gVisor/Firecracker-capable host. |
| Tests | `env GOCACHE=/private/tmp/nanovms-gocache go test ./...` | Pass, exit 0, 18.3s | Only `internal/adapters/sandbox` and `internal/domain` have Go test files; most packages report `[no test files]`. |

## LOC

- Tracked Go LOC: 4,264.
- Largest Go files:
  - `internal/adapters/sandbox/sandbox.go`: 979 lines.
  - `internal/adapters/mac/mac.go`: 401 lines.
  - `internal/adapters/sandbox/sandbox_test.go`: 330 lines.
  - `internal/adapters/windows/windows.go`: 325 lines.
  - `internal/adapters/linux/linux.go`: 286 lines.
  - `internal/domain/sandbox.go`: 253 lines.
  - `internal/adapters/wasm/wasm.go`: 212 lines.
  - `pkg/validate/validate.go`: 210 lines.
- Documentation and support files are larger than core Go in several places, especially ADRs:
  - `SPEC.md`: 89,676 bytes.
  - `docs/adr/ADR-014-agent-desktop-environments.md`: 765 lines.
  - `docs/adr/ADR-013-game-automation-testing.md`: 673 lines.
  - `docs/adr/ADR-012-multi-tenancy.md`: 473 lines.
- File-size risk: `internal/adapters/sandbox/sandbox.go` exceeds the 500-line repository guideline and mixes gVisor, Landlock, seccomp, Wasmtime, generic adapter state, logs, exec, metrics, and support helpers.

## Top 20 TODOs

1. Split `internal/adapters/sandbox/sandbox.go` into focused adapter modules before adding more sandbox behavior.
2. Replace `landlockAdapter.Get` placeholder behavior with real PID/state tracking or remove unsupported API surface.
3. Replace `landlockAdapter.Exec` guidance-only error with a concrete nsenter/PID-tracking implementation or mark unsupported in the port contract.
4. Implement `orchestrate.Engine.Stop` for Tier 1 instead of returning `tier1 stop not yet implemented`.
5. Implement `orchestrate.Engine.Stop` for Tier 2 instead of returning `tier2 stop not yet implemented`.
6. Implement `orchestrate.Engine.Delete` for Tier 1 instead of returning `tier1 delete not yet implemented`.
7. Implement `orchestrate.Engine.Delete` for Tier 2 instead of returning `tier2 delete not yet implemented`.
8. Make `internal/adapters/linux.Adapter.createMicroVM` launch or persist Firecracker/Cloud Hypervisor state instead of only constructing an unused config map.
9. Make `internal/adapters/linux.Adapter.startMicroVM` perform a real start operation instead of returning nil.
10. Replace MicroVM exec fallback in `internal/adapters/linux.Adapter.execMicroVM`; it currently runs a host shell command, not a VM command.
11. Replace WASM create/start no-op semantics with explicit module validation, instantiation, and lifecycle state.
12. Add tests for `pkg/orchestrate`; it is central routing logic and currently has no package tests.
13. Add tests for `pkg/runtime`; tier registry and runtime selection currently have no package tests.
14. Add tests for `pkg/validate`; config validation is user-facing and currently has no package tests.
15. Add tests for `internal/adapters/linux`, especially Linux primary target behavior and missing-tool failures.
16. Add tests for CLI behavior in `cmd/nvms` and `cmd/nanovms`; both packages currently report no test files.
17. Convert `TEST_COVERAGE_MATRIX.md` TBD rows into actual package/function coverage targets or remove stale placeholders.
18. Decide whether `sdk/rust` no-op renderer/audio placeholders are intentional test doubles or incomplete SDK surface.
19. Reconcile README install/build examples that reference `KooshaPari/nvms` with the module path `github.com/kooshapari/nanovms`.
20. Run a Linux-hosted validation pass for gVisor, Landlock, seccomp, and Firecracker paths; macOS build success does not validate the primary runtime substrate.

## Cross-cutting deps (phenocompose-nanovms-pine world map)

Current world map from repository evidence:

```text
phenocompose / pheno-compose
  |
  | documented Rust driver boundary
  v
integrations/pheno-compose/README.md
  |
  | expects: NVMS_DRIVER_ENDPOINT, NVMS_DRIVER_TIER, NVMS_DRIVER_TIMEOUT
  | expects: pheno-compose deploy --runtime nvms --config nvms.yaml
  v
cmd/nvms -> pkg/deploy -> pkg/config -> pkg/orchestrate
                                      |
                                      v
                              pkg/runtime / pkg/tier
                                      |
          +---------------------------+---------------------------+
          |                           |                           |
          v                           v                           v
      Tier 1 WASM                 Tier 2 gVisor              Tier 3 Firecracker
      pkg/tier/wasm.go            pkg/tier/gvisor.go         pkg/tier/firecracker.go
      internal/adapters/wasm      sandbox/runsc paths        Linux MicroVM / KVM
          |                           |                           |
          +---------------------------+---------------------------+
                                      |
                                      v
                           internal/domain + internal/ports
                                      |
                                      v
                    platform adapters: linux, mac, windows, sandbox
```

- Cross-cutting contract: `internal/domain.SandboxConfig`, `internal/domain.Sandbox`, and `internal/ports` define the shared object model.
- Cross-cutting routing: `pkg/orchestrate.Engine` maps tier integers to concrete tier adapters.
- Cross-cutting validation: `pkg/validate` validates tier, image, resources, mounts, and sandbox type.
- Cross-cutting config: `pkg/config` loads `nvms.yaml` and converts it to the domain config consumed by orchestration.
- Cross-cutting external dependency: `go.mod` currently has only one non-stdlib Go module dependency, `gopkg.in/yaml.v3 v3.0.1`.
- PhenoCompose state: the repository has a documented integration boundary, but this audit did not find an executable PhenoCompose driver implementation inside this repo.
- Pine state: no concrete `pine` package, binary, or integration was found by repository search; any `phenocompose-nanovms-pine` map should currently treat Pine as an external or not-yet-imported world component.

## Linux primary target

- Linux is the primary runtime validation target because Tier 2 gVisor, Landlock/seccomp, and Tier 3 Firecracker depend on Linux kernel/runtime features.
- CI is configured around Ubuntu runners and Go 1.23, which matches the primary target direction.
- Local audit ran on `darwin/arm64`; it proves compile/test/vet portability on this host but not Linux runtime correctness.
- Linux adapter checks for `ip`, `unshare`, `mount`, `firecracker`, `cloud-hypervisor`, and `wasmtime`.
- Runtime gaps to validate on Linux:
  - KVM and Firecracker launch/state persistence.
  - gVisor `runsc` create/start/logs/exec behavior.
  - Landlock/seccomp lifecycle state and observability.
  - Namespace execution behavior without host-shell fallback leakage.
  - Permission model under rootless and privileged modes.

## Quality gate (go vet)

- Command: `env GOCACHE=/private/tmp/nanovms-gocache go vet ./...`
- Result: pass, exit 0, 20.1s.
- Companion command: `env GOCACHE=/private/tmp/nanovms-gocache go test ./...` passed.
- Caveat: `go vet` is a static gate only; it does not validate the unavailable local Linux runtime dependencies.
- Tooling note: all local commands were prefixed with `/Users/kooshapari/go/bin/snip --`; `snip` repeatedly emitted readonly tracking warnings, but command exit statuses above are from the wrapped command output.

## 5 prioritized next steps

1. Decompose `internal/adapters/sandbox/sandbox.go` into separate `gvisor`, `landlock`, `seccomp`, `wasmtime`, and shared state/helper files, then rerun `go test ./...`, `go vet ./...`, and `go build ./...`.
2. Make the Linux primary target real in CI: add a Linux integration job or scripted local runner that checks required binaries/kernel features and exercises Tier 1, Tier 2, and Tier 3 smoke flows.
3. Close lifecycle holes in orchestration: implement Tier 1/Tier 2 stop/delete and make Tier 3 lifecycle state observable through the same interface.
4. Turn PhenoCompose integration from documentation into an executable contract: define the driver API, add fixtures for `nvms.yaml`, and add a smoke test that proves `pheno-compose deploy --runtime nvms` maps into `cmd/nvms/pkg/orchestrate`.
5. Raise package coverage around the routing surface first: add focused tests for `pkg/orchestrate`, `pkg/runtime`, `pkg/validate`, `internal/adapters/linux`, and the CLI entrypoints.
