# Session Overview

## Goal

Add the Sladge disclosure badge to `nanovms` without touching unrelated local work in the canonical checkout.

## Outcome

- Added the `AI Slop Inside` badge to the README badge block.
- Used `nanovms-wtrees/sladge-badge` because canonical `nanovms` has unrelated README, spec, coverage, and worklog changes.
- Kept Go source, isolation runtime, specs, coverage matrices, and worklogs out of scope.
